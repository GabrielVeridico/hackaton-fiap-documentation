# UserAPI (HackatonFiap.Users) Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Transformar o boilerplate `FGC.Users` no microsserviço de Identidade & Acesso do Conexão Solidária (cadastro de Doador PF/PJ, login/refresh/logout com JWT, gestão de usuários com Owner/matriz de permissões), em .NET 8, Clean Architecture, CQRS manual, EF Core + SQL Server, OpenTelemetry.

**Architecture:** 4 projetos `src/` (Domain/Application/Infrastructure/API) + `tests/`. Handlers CQRS manuais retornando `Result<T>`, injetados direto nos controllers. Refresh token rotativo com hash SHA-256 e detecção de reuso. Observabilidade OpenTelemetry (traces + métricas) com exporter Prometheus em `/metrics`.

**Tech Stack:** .NET 8, EF Core 8 (SQL Server), JWT Bearer, BCrypt.Net, FluentValidation, Serilog, OpenTelemetry, xUnit + NSubstitute + FluentAssertions.

**Spec:** `docs/superpowers/specs/2026-06-16-userapi-design.md`

**Base dir (todos os caminhos relativos a):** `C:\eSolution\Projetos\hackaton-fiap\hackaton-fiap-users`

**Convenção de commit/branch:** trabalhar numa branch `feature/userapi-conexao-solidaria`. Commits frequentes em pt-BR com prefixo convencional. Este repo (`hackaton-fiap-users`) tem `.git` próprio.

---

## File Structure (visão geral)

```
src/HackatonFiap.Users.Domain/
  Enums/PersonType.cs                (novo)
  Enums/UserRole.cs                  (alterado: Doador/GestorONG)
  ValueObjects/Document.cs           (novo: CPF/CNPJ + dígitos)
  ValueObjects/Password.cs           (mantido)
  Entities/User.cs                   (reescrito)
  Entities/RefreshToken.cs           (novo)
  Abstractions/{Result,ResultT,Error}.cs (mantidos)
src/HackatonFiap.Users.Application/
  Commands/RegisterDonor/*           (novo)
  Commands/AuthenticateUser/*        (estendido)
  Commands/RefreshToken/*            (novo)
  Commands/Logout/*                  (novo)
  Commands/CreateUser/*              (reescrito p/ admin)
  Commands/UpdateUser/*              (novo)
  Commands/ChangeUserRole/*          (novo)
  Commands/DeactivateUser/*          (novo)
  Commands/ReactivateUser/*          (novo)
  Commands/UpdateMyProfile/*         (renomeado de UpdateProfile)
  Commands/ResetMyPassword/*         (novo)
  Queries/GetProfile/*               (mantido)
  Queries/GetUserById/*              (novo)
  Queries/ListUsers/*                (novo)
  DTOs/{AuthResponse,AccessToken,UserResponse}.cs
  Interfaces/{IUserRepository,IRefreshTokenRepository,IJwtTokenGenerator,IRefreshTokenService,IPasswordHasher,IAuditService}.cs
  Errors/UserErrors.cs               (estendido)
src/HackatonFiap.Users.Infrastructure/
  Persistence/ApplicationDbContext.cs        (+ RefreshTokens)
  Persistence/Configurations/{UserConfiguration,RefreshTokenConfiguration}.cs
  Persistence/Repositories/{UserRepository,RefreshTokenRepository}.cs
  Identity/{JwtTokenGenerator,RefreshTokenService}.cs
  Migrations/* (regenerado)
src/HackatonFiap.Users.API/
  Controllers/{AuthController,UsersController,HealthController}.cs
  Program.cs                          (+ OTel, + DI)
  Observability/Telemetry.cs          (novo: ActivitySource/Meter)
tests/HackatonFiap.Users.UnitTests/   (NSubstitute)
```

---

## Task 0: Rename `FGC.Users` → `HackatonFiap.Users`

**Files:** todos os `.cs`, `.csproj`, `.sln`, `Dockerfile`, `docker-compose.yml`, `k8s/*.yaml`, `launchSettings.json`, `CLAUDE.md`.

- [ ] **Step 1: Criar a branch**

```powershell
git checkout -b feature/userapi-conexao-solidaria
```

- [ ] **Step 2: Renomear pastas dos projetos e do .sln**

```powershell
Get-ChildItem -Recurse -Directory -Filter "FGC.Users*" |
  Sort-Object FullName -Descending |
  ForEach-Object { Rename-Item $_.FullName ($_.Name -replace '^FGC\.Users','HackatonFiap.Users') }
Get-ChildItem -Recurse -File -Filter "FGC.Users*" |
  ForEach-Object { Rename-Item $_.FullName ($_.Name -replace '^FGC\.Users','HackatonFiap.Users') }
Rename-Item "FGC.Users.sln" "HackatonFiap.Users.sln"
```

- [ ] **Step 3: Substituir o texto `FGC.Users` → `HackatonFiap.Users` em todos os arquivos de texto**

```powershell
$exts = '*.cs','*.csproj','*.sln','*.json','*.yaml','*.yml','*.md','Dockerfile'
Get-ChildItem -Recurse -File -Include $exts |
  Where-Object { $_.FullName -notmatch '\\(bin|obj|\.git)\\' } |
  ForEach-Object {
    (Get-Content -Raw $_.FullName) -replace 'FGC\.Users','HackatonFiap.Users' |
      Set-Content -NoNewline $_.FullName
  }
```

> Também substituir a string `"FGC.Users"` do `Enrich.WithProperty("ServiceName", ...)` no `Program.cs` e o seed `superadmin@fgc.com` — serão tratados nas tasks de seed/observabilidade. O `Jwt:Issuer`/`Audience` (`fgc.local`/`fgc.clients`) podem permanecer; serão revistos na Task 14.

- [ ] **Step 4: Restaurar e compilar**

Run: `dotnet build HackatonFiap.Users.sln`
Expected: **Build succeeded** (0 errors).

- [ ] **Step 5: Rodar os testes existentes (ainda em Moq)**

Run: `dotnet test`
Expected: PASS (42 testes).

- [ ] **Step 6: Commit**

```powershell
git add -A
git commit -m "refactor: renomeia FGC.Users para HackatonFiap.Users"
```

---

## Task 1: Trocar framework de mock Moq → NSubstitute

**Files:**
- Modify: `tests/HackatonFiap.Users.UnitTests/HackatonFiap.Users.UnitTests.csproj`
- Modify: todos os arquivos em `tests/HackatonFiap.Users.UnitTests/Commands/` e `Queries/` que usam `Moq`.

- [ ] **Step 1: Substituir o pacote no csproj**

Em `HackatonFiap.Users.UnitTests.csproj`, trocar a linha do Moq por NSubstitute:

```xml
    <PackageReference Include="NSubstitute" Version="5.1.0" />
```

(remover `<PackageReference Include="Moq" Version="4.20.70" />`)

- [ ] **Step 2: Converter a sintaxe nos testes existentes**

Padrões de conversão (aplicar em cada arquivo de teste):

```
// Moq                                          -> NSubstitute
new Mock<IFoo>()                                -> Substitute.For<IFoo>()
_foo.Object                                     -> _foo
_foo.Setup(x => x.Bar(a)).ReturnsAsync(v)       -> _foo.Bar(a).Returns(Task.FromResult(v))   // ou .Returns(v) p/ async via ReturnsAsync helper
_foo.Setup(x => x.Bar(a)).Returns(v)            -> _foo.Bar(a).Returns(v)
_foo.Verify(x => x.Bar(a), Times.Once)          -> _foo.Received(1).Bar(a)
_foo.Verify(x => x.Bar(a), Times.Never)         -> _foo.DidNotReceive().Bar(a)
```

Exemplo — `AuthenticateUserCommandHandlerTests.cs` convertido:

```csharp
using Xunit;
using HackatonFiap.Users.Application.Commands.AuthenticateUser;
using HackatonFiap.Users.Application.DTOs;
using HackatonFiap.Users.Application.Errors;
using HackatonFiap.Users.Application.Interfaces;
using HackatonFiap.Users.Domain.Entities;
using HackatonFiap.Users.Domain.ValueObjects;
using FluentAssertions;
using NSubstitute;

namespace HackatonFiap.Users.UnitTests.Commands;

public class AuthenticateUserCommandHandlerTests
{
    private readonly IUserRepository _userRepository = Substitute.For<IUserRepository>();
    private readonly IPasswordHasher _passwordHasher = Substitute.For<IPasswordHasher>();
    private readonly IJwtTokenGenerator _jwtTokenGenerator = Substitute.For<IJwtTokenGenerator>();
    // ... handler instanciado no ctor (mesma estrutura, sem .Object)
}
```

> Nota: este teste será **reescrito** na Task 8 (Authenticate passa a emitir refresh token). Aqui apenas garantimos build verde após a troca de pacote. Converter os 4 arquivos de teste de handler (`AuthenticateUser`, `CreateUser`, `UpdateProfile`, `GetProfile`) e o de validator se usar Moq.

- [ ] **Step 3: Restaurar e testar**

Run: `dotnet test`
Expected: PASS (mesma contagem de testes).

- [ ] **Step 4: Commit**

```powershell
git add -A
git commit -m "test: migra mocks de Moq para NSubstitute"
```

---

## Task 2: Enums de domínio (`PersonType`, `UserRole`)

**Files:**
- Create: `src/HackatonFiap.Users.Domain/Enums/PersonType.cs`
- Modify: `src/HackatonFiap.Users.Domain/Enums/UserRole.cs`
- Modify: `src/HackatonFiap.Users.API/Program.cs` (seed usa `UserRole.Admin`)

- [ ] **Step 1: Criar `PersonType`**

```csharp
namespace HackatonFiap.Users.Domain.Enums;

public enum PersonType
{
    Individual = 0, // Pessoa Física (PF) — CPF
    Company = 1     // Pessoa Jurídica (PJ) — CNPJ
}
```

- [ ] **Step 2: Alterar `UserRole` (valores em pt-BR)**

```csharp
namespace HackatonFiap.Users.Domain.Enums;

public enum UserRole
{
    Doador = 0,
    GestorONG = 1
}
```

- [ ] **Step 3: Ajustar o seed no `Program.cs`**

Trocar `UserRole.Admin` por `UserRole.GestorONG` na criação do usuário seed (o seed será totalmente reescrito como Owner na Task 11; aqui só fazer compilar).

- [ ] **Step 4: Compilar**

Run: `dotnet build`
Expected: pode falhar em testes/handlers que referenciam roles antigas — corrigir referências de `UserRole.User`/`UserRole.Admin` restantes para `Doador`/`GestorONG`. Build deve terminar **succeeded**.

- [ ] **Step 5: Commit**

```powershell
git add -A
git commit -m "feat(domain): roles Doador/GestorONG e enum PersonType"
```

---

## Task 3: Value Object `Document` (CPF/CNPJ com dígitos verificadores)

**Files:**
- Create: `src/HackatonFiap.Users.Domain/ValueObjects/Document.cs`
- Test: `tests/HackatonFiap.Users.UnitTests/Domain/DocumentTests.cs`

- [ ] **Step 1: Escrever os testes (falhando)**

```csharp
using HackatonFiap.Users.Domain.Enums;
using HackatonFiap.Users.Domain.ValueObjects;
using FluentAssertions;
using Xunit;

namespace HackatonFiap.Users.UnitTests.Domain;

public class DocumentTests
{
    [Theory]
    [InlineData("529.982.247-25")]
    [InlineData("52998224725")]
    public void IsValid_WithValidCpf_ReturnsTrue(string cpf)
    {
        Document.IsValid(cpf, PersonType.Individual).Should().BeTrue();
    }

    [Theory]
    [InlineData("111.111.111-11")] // todos iguais
    [InlineData("529.982.247-24")] // dígito errado
    [InlineData("123")]            // tamanho errado
    public void IsValid_WithInvalidCpf_ReturnsFalse(string cpf)
    {
        Document.IsValid(cpf, PersonType.Individual).Should().BeFalse();
    }

    [Theory]
    [InlineData("11.444.777/0001-61")]
    [InlineData("11444777000161")]
    public void IsValid_WithValidCnpj_ReturnsTrue(string cnpj)
    {
        Document.IsValid(cnpj, PersonType.Company).Should().BeTrue();
    }

    [Theory]
    [InlineData("11.111.111/1111-11")]
    [InlineData("11.444.777/0001-60")]
    public void IsValid_WithInvalidCnpj_ReturnsFalse(string cnpj)
    {
        Document.IsValid(cnpj, PersonType.Company).Should().BeFalse();
    }

    [Fact]
    public void Create_NormalizesToDigitsOnly()
    {
        Document.Create("529.982.247-25", PersonType.Individual).Value.Should().Be("52998224725");
    }
}
```

- [ ] **Step 2: Rodar e ver falhar**

Run: `dotnet test --filter "FullyQualifiedName~DocumentTests"`
Expected: FAIL (Document não existe).

- [ ] **Step 3: Implementar `Document`**

```csharp
using System.Linq;
using HackatonFiap.Users.Domain.Enums;

namespace HackatonFiap.Users.Domain.ValueObjects;

public sealed class Document
{
    public string Value { get; private set; }

    private Document() => Value = string.Empty; // EF

    private Document(string value) => Value = value;

    public static Document Create(string raw, PersonType type) => new(Normalize(raw));

    public static bool IsValid(string raw, PersonType type)
    {
        var digits = Normalize(raw);
        return type == PersonType.Individual ? IsValidCpf(digits) : IsValidCnpj(digits);
    }

    private static string Normalize(string raw) =>
        new string((raw ?? string.Empty).Where(char.IsDigit).ToArray());

    private static bool IsValidCpf(string cpf)
    {
        if (cpf.Length != 11 || cpf.Distinct().Count() == 1)
            return false;

        var digits = cpf.Select(c => c - '0').ToArray();

        var d1 = CheckDigit(digits, 9, 10);
        var d2 = CheckDigit(digits, 10, 11);

        return digits[9] == d1 && digits[10] == d2;
    }

    private static int CheckDigit(int[] digits, int count, int startWeight)
    {
        var sum = 0;
        for (var i = 0; i < count; i++)
            sum += digits[i] * (startWeight - i);

        var rem = sum % 11;
        return rem < 2 ? 0 : 11 - rem;
    }

    private static bool IsValidCnpj(string cnpj)
    {
        if (cnpj.Length != 14 || cnpj.Distinct().Count() == 1)
            return false;

        var digits = cnpj.Select(c => c - '0').ToArray();
        int[] w1 = { 5, 4, 3, 2, 9, 8, 7, 6, 5, 4, 3, 2 };
        int[] w2 = { 6, 5, 4, 3, 2, 9, 8, 7, 6, 5, 4, 3, 2 };

        var d1 = CnpjDigit(digits, w1);
        var d2 = CnpjDigit(digits, w2);

        return digits[12] == d1 && digits[13] == d2;
    }

    private static int CnpjDigit(int[] digits, int[] weights)
    {
        var sum = 0;
        for (var i = 0; i < weights.Length; i++)
            sum += digits[i] * weights[i];

        var rem = sum % 11;
        return rem < 2 ? 0 : 11 - rem;
    }
}
```

- [ ] **Step 4: Rodar e ver passar**

Run: `dotnet test --filter "FullyQualifiedName~DocumentTests"`
Expected: PASS.

- [ ] **Step 5: Commit**

```powershell
git add -A
git commit -m "feat(domain): VO Document com validacao de CPF/CNPJ"
```

---

## Task 4: Reescrever a entidade `User` + EF config + DTOs

**Files:**
- Modify: `src/HackatonFiap.Users.Domain/Entities/User.cs`
- Modify: `src/HackatonFiap.Users.Infrastructure/Persistence/Configurations/UserConfiguration.cs`
- Modify: `src/HackatonFiap.Users.Application/DTOs/UserResponse.cs`
- Modify: handlers/tests/seed que usavam `User.Create(...)` (CreateUser, UpdateProfile, GetProfile, AuthenticateUser e seus testes; Program.cs seed)
- Test: `tests/HackatonFiap.Users.UnitTests/Domain/UserEntityTests.cs` (reescrito)

- [ ] **Step 1: Reescrever os testes da entidade (falhando)**

```csharp
using HackatonFiap.Users.Domain.Entities;
using HackatonFiap.Users.Domain.Enums;
using HackatonFiap.Users.Domain.ValueObjects;
using FluentAssertions;
using Xunit;

namespace HackatonFiap.Users.UnitTests.Domain;

public class UserEntityTests
{
    private static Document Cpf() => Document.Create("52998224725", PersonType.Individual);
    private static Password Hash() => new("hashed");

    [Fact]
    public void RegisterDonor_SetsRoleDoadorAndActive()
    {
        var user = User.RegisterDonor(PersonType.Individual, Cpf(), "João", "joao@x.com", Hash());

        user.Role.Should().Be(UserRole.Doador);
        user.IsActive.Should().BeTrue();
        user.IsOwner.Should().BeFalse();
        user.Id.Should().NotBe(Guid.Empty);
    }

    [Fact]
    public void Deactivate_SetsInactive()
    {
        var user = User.RegisterDonor(PersonType.Individual, Cpf(), "João", "joao@x.com", Hash());
        user.Deactivate();
        user.IsActive.Should().BeFalse();
    }

    [Fact]
    public void Reactivate_SetsActive()
    {
        var user = User.RegisterDonor(PersonType.Individual, Cpf(), "João", "joao@x.com", Hash());
        user.Deactivate();
        user.Reactivate();
        user.IsActive.Should().BeTrue();
    }

    [Fact]
    public void ChangeRole_UpdatesRole()
    {
        var user = User.RegisterDonor(PersonType.Individual, Cpf(), "João", "joao@x.com", Hash());
        user.ChangeRole(UserRole.GestorONG);
        user.Role.Should().Be(UserRole.GestorONG);
    }

    [Fact]
    public void CreateOwner_IsOwnerGestorAndActive()
    {
        var user = User.CreateOwner(Cpf(), "Owner", "owner@x.com", Hash());
        user.IsOwner.Should().BeTrue();
        user.Role.Should().Be(UserRole.GestorONG);
        user.IsActive.Should().BeTrue();
    }
}
```

- [ ] **Step 2: Rodar e ver falhar**

Run: `dotnet test --filter "FullyQualifiedName~UserEntityTests"`
Expected: FAIL (métodos não existem).

- [ ] **Step 3: Reescrever `User.cs`**

```csharp
using HackatonFiap.Users.Domain.Enums;
using HackatonFiap.Users.Domain.ValueObjects;

namespace HackatonFiap.Users.Domain.Entities;

public class User
{
    public Guid Id { get; private set; }
    public PersonType PersonType { get; private set; }
    public Document Document { get; private set; } = null!;
    public string Name { get; private set; } = null!;
    public string Email { get; private set; } = null!;
    public Password Password { get; private set; } = null!;
    public UserRole Role { get; private set; }
    public bool IsActive { get; private set; }
    public bool IsOwner { get; private set; }
    public DateTime CreatedAtUtc { get; private set; }
    public DateTime UpdatedAtUtc { get; private set; }

    private User() { }

    public static User RegisterDonor(PersonType personType, Document document, string name, string email, Password password)
        => Build(personType, document, name, email, password, UserRole.Doador, isOwner: false);

    public static User CreateByAdmin(PersonType personType, Document document, string name, string email, Password password, UserRole role)
        => Build(personType, document, name, email, password, role, isOwner: false);

    public static User CreateOwner(Document document, string name, string email, Password password)
        => Build(PersonType.Individual, document, name, email, password, UserRole.GestorONG, isOwner: true);

    private static User Build(PersonType personType, Document document, string name, string email, Password password, UserRole role, bool isOwner)
    {
        var now = DateTime.UtcNow;
        return new User
        {
            Id = Guid.NewGuid(),
            PersonType = personType,
            Document = document,
            Name = name,
            Email = email,
            Password = password,
            Role = role,
            IsActive = true,
            IsOwner = isOwner,
            CreatedAtUtc = now,
            UpdatedAtUtc = now
        };
    }

    public void UpdateProfile(string name)
    {
        Name = name;
        Touch();
    }

    public void ChangePassword(Password password)
    {
        Password = password;
        Touch();
    }

    public void ChangeRole(UserRole role)
    {
        Role = role;
        Touch();
    }

    public void Deactivate()
    {
        IsActive = false;
        Touch();
    }

    public void Reactivate()
    {
        IsActive = true;
        Touch();
    }

    private void Touch() => UpdatedAtUtc = DateTime.UtcNow;
}
```

- [ ] **Step 4: Atualizar `UserConfiguration`**

```csharp
using HackatonFiap.Users.Domain.Entities;
using Microsoft.EntityFrameworkCore;
using Microsoft.EntityFrameworkCore.Metadata.Builders;

namespace HackatonFiap.Users.Infrastructure.Persistence.Configurations;

public class UserConfiguration : IEntityTypeConfiguration<User>
{
    public void Configure(EntityTypeBuilder<User> builder)
    {
        builder.ToTable("Users");
        builder.HasKey(u => u.Id);

        builder.Property(u => u.Name).IsRequired().HasMaxLength(200);
        builder.Property(u => u.Email).IsRequired().HasMaxLength(200);
        builder.HasIndex(u => u.Email).IsUnique();

        builder.Property(u => u.PersonType).HasConversion<string>().HasMaxLength(20);
        builder.Property(u => u.Role).HasConversion<string>().HasMaxLength(50);
        builder.Property(u => u.IsActive).HasDefaultValue(true);
        builder.Property(u => u.IsOwner).HasDefaultValue(false);

        builder.OwnsOne(u => u.Document, doc =>
        {
            doc.Property(d => d.Value).HasColumnName("Document").IsRequired().HasMaxLength(14);
            doc.HasIndex(d => d.Value).IsUnique();
        });

        builder.OwnsOne(u => u.Password, pw =>
        {
            pw.Property(p => p.HashValue).HasColumnName("PasswordHash").IsRequired();
        });

        builder.HasQueryFilter(u => u.IsActive);
    }
}
```

> Nota EF: o índice único em propriedade de owned type requer EF Core 8 (suportado). O global query filter em `IsActive` continua — repos de unicidade/reativação usarão `IgnoreQueryFilters()` (Task 7/11).

- [ ] **Step 5: Atualizar `UserResponse` DTO**

```csharp
namespace HackatonFiap.Users.Application.DTOs;

public record UserResponse(
    Guid Id,
    string PersonType,
    string Document,
    string Name,
    string Email,
    string Role,
    bool IsActive,
    bool IsOwner,
    DateTime CreatedAtUtc,
    DateTime UpdatedAtUtc);
```

- [ ] **Step 6: Corrigir handlers/DTOs/seed que quebraram**

Ajustar para compilar (serão refinados nas tasks de feature):
- `CreateUserCommandHandler`, `UpdateProfileCommandHandler`, `GetProfileQueryHandler`: trocar `User.Create(...)` e a montagem de `UserResponse` para a nova forma. Onde precisar de `Document`/`PersonType`, usar os campos novos.
- `Program.cs` seed: usar `User.CreateOwner(Document.Create(...), ...)` provisoriamente (reescrito na Task 11).
- Testes existentes que chamam `User.Create(...)`: trocar para `User.RegisterDonor(...)`.

- [ ] **Step 7: Build + testes**

Run: `dotnet build` então `dotnet test --filter "FullyQualifiedName~UserEntityTests"`
Expected: build succeeded; UserEntityTests PASS.

- [ ] **Step 8: Commit**

```powershell
git add -A
git commit -m "feat(domain): reescreve User (PF/PJ, Document, Owner, soft-delete)"
```

---

## Task 5: Entidade `RefreshToken` + EF config + repositório

**Files:**
- Create: `src/HackatonFiap.Users.Domain/Entities/RefreshToken.cs`
- Create: `src/HackatonFiap.Users.Infrastructure/Persistence/Configurations/RefreshTokenConfiguration.cs`
- Modify: `src/HackatonFiap.Users.Infrastructure/Persistence/ApplicationDbContext.cs`
- Create: `src/HackatonFiap.Users.Application/Interfaces/IRefreshTokenRepository.cs`
- Create: `src/HackatonFiap.Users.Infrastructure/Persistence/Repositories/RefreshTokenRepository.cs`
- Test: `tests/HackatonFiap.Users.UnitTests/Domain/RefreshTokenTests.cs`

- [ ] **Step 1: Teste da entidade (falhando)**

```csharp
using HackatonFiap.Users.Domain.Entities;
using FluentAssertions;
using Xunit;

namespace HackatonFiap.Users.UnitTests.Domain;

public class RefreshTokenTests
{
    [Fact]
    public void Issue_IsActive()
    {
        var t = RefreshToken.Issue(Guid.NewGuid(), "hash", DateTime.UtcNow.AddDays(7));
        t.IsActive.Should().BeTrue();
    }

    [Fact]
    public void Revoke_SetsRevokedAndInactive()
    {
        var t = RefreshToken.Issue(Guid.NewGuid(), "hash", DateTime.UtcNow.AddDays(7));
        var newId = Guid.NewGuid();
        t.Revoke(newId);
        t.RevokedAtUtc.Should().NotBeNull();
        t.ReplacedByTokenId.Should().Be(newId);
        t.IsActive.Should().BeFalse();
    }

    [Fact]
    public void Expired_IsInactive()
    {
        var t = RefreshToken.Issue(Guid.NewGuid(), "hash", DateTime.UtcNow.AddSeconds(-1));
        t.IsActive.Should().BeFalse();
    }
}
```

- [ ] **Step 2: Rodar e ver falhar** — `dotnet test --filter "FullyQualifiedName~RefreshTokenTests"` → FAIL.

- [ ] **Step 3: Implementar `RefreshToken`**

```csharp
namespace HackatonFiap.Users.Domain.Entities;

public class RefreshToken
{
    public Guid Id { get; private set; }
    public Guid UserId { get; private set; }
    public string TokenHash { get; private set; } = null!;
    public DateTime ExpiresAtUtc { get; private set; }
    public DateTime CreatedAtUtc { get; private set; }
    public DateTime? RevokedAtUtc { get; private set; }
    public Guid? ReplacedByTokenId { get; private set; }

    private RefreshToken() { }

    public static RefreshToken Issue(Guid userId, string tokenHash, DateTime expiresAtUtc) => new()
    {
        Id = Guid.NewGuid(),
        UserId = userId,
        TokenHash = tokenHash,
        ExpiresAtUtc = expiresAtUtc,
        CreatedAtUtc = DateTime.UtcNow
    };

    public bool IsActive => RevokedAtUtc is null && ExpiresAtUtc > DateTime.UtcNow;

    public void Revoke(Guid? replacedByTokenId = null)
    {
        if (RevokedAtUtc is null)
            RevokedAtUtc = DateTime.UtcNow;
        ReplacedByTokenId = replacedByTokenId;
    }
}
```

- [ ] **Step 4: `ApplicationDbContext` + `RefreshTokenConfiguration`**

DbContext — adicionar `public DbSet<RefreshToken> RefreshTokens { get; set; } = null!;`

```csharp
using HackatonFiap.Users.Domain.Entities;
using Microsoft.EntityFrameworkCore;
using Microsoft.EntityFrameworkCore.Metadata.Builders;

namespace HackatonFiap.Users.Infrastructure.Persistence.Configurations;

public class RefreshTokenConfiguration : IEntityTypeConfiguration<RefreshToken>
{
    public void Configure(EntityTypeBuilder<RefreshToken> builder)
    {
        builder.ToTable("RefreshTokens");
        builder.HasKey(t => t.Id);
        builder.Property(t => t.TokenHash).IsRequired().HasMaxLength(128);
        builder.HasIndex(t => t.TokenHash).IsUnique();
        builder.HasIndex(t => t.UserId);
    }
}
```

- [ ] **Step 5: `IRefreshTokenRepository` + `RefreshTokenRepository`**

```csharp
using HackatonFiap.Users.Domain.Entities;

namespace HackatonFiap.Users.Application.Interfaces;

public interface IRefreshTokenRepository
{
    Task AddAsync(RefreshToken token);
    Task<RefreshToken?> FindByHashAsync(string tokenHash);
    Task<IReadOnlyList<RefreshToken>> FindActiveByUserAsync(Guid userId);
    Task SaveChangesAsync();
}
```

```csharp
using HackatonFiap.Users.Application.Interfaces;
using HackatonFiap.Users.Domain.Entities;
using Microsoft.EntityFrameworkCore;

namespace HackatonFiap.Users.Infrastructure.Persistence.Repositories;

public class RefreshTokenRepository : IRefreshTokenRepository
{
    private readonly ApplicationDbContext _db;
    public RefreshTokenRepository(ApplicationDbContext db) => _db = db;

    public async Task AddAsync(RefreshToken token)
    {
        await _db.RefreshTokens.AddAsync(token);
        await _db.SaveChangesAsync();
    }

    public Task<RefreshToken?> FindByHashAsync(string tokenHash) =>
        _db.RefreshTokens.FirstOrDefaultAsync(t => t.TokenHash == tokenHash);

    public async Task<IReadOnlyList<RefreshToken>> FindActiveByUserAsync(Guid userId) =>
        await _db.RefreshTokens
            .Where(t => t.UserId == userId && t.RevokedAtUtc == null && t.ExpiresAtUtc > DateTime.UtcNow)
            .ToListAsync();

    public Task SaveChangesAsync() => _db.SaveChangesAsync();
}
```

- [ ] **Step 6: Build + testes** — `dotnet test --filter "FullyQualifiedName~RefreshTokenTests"` → PASS.

- [ ] **Step 7: Commit**

```powershell
git add -A
git commit -m "feat: entidade RefreshToken, config EF e repositorio"
```

---

## Task 6: Serviços de token (access JWT 4h + refresh) e DTOs de auth

**Files:**
- Create: `src/HackatonFiap.Users.Application/DTOs/AccessToken.cs`, `AuthResponse.cs`
- Modify: `src/HackatonFiap.Users.Application/Interfaces/IJwtTokenGenerator.cs`
- Create: `src/HackatonFiap.Users.Application/Interfaces/IRefreshTokenService.cs`
- Modify: `src/HackatonFiap.Users.Infrastructure/Identity/JwtTokenGenerator.cs`
- Create: `src/HackatonFiap.Users.Infrastructure/Identity/RefreshTokenService.cs`
- Remove: `src/HackatonFiap.Users.Application/DTOs/LoginResponse.cs` (substituído)

- [ ] **Step 1: DTOs**

```csharp
namespace HackatonFiap.Users.Application.DTOs;
public record AccessToken(string Token, DateTime ExpiresAtUtc);
```
```csharp
namespace HackatonFiap.Users.Application.DTOs;
public record AuthResponse(string AccessToken, string RefreshToken, int ExpiresIn);
```

- [ ] **Step 2: Interfaces**

```csharp
using HackatonFiap.Users.Application.DTOs;
using HackatonFiap.Users.Domain.Entities;

namespace HackatonFiap.Users.Application.Interfaces;

public interface IJwtTokenGenerator
{
    AccessToken GenerateAccessToken(User user);
}
```
```csharp
namespace HackatonFiap.Users.Application.Interfaces;

public interface IRefreshTokenService
{
    // gera um token aleatório; devolve o valor cru (vai pro cliente) e o hash (vai pro banco)
    (string RawToken, string TokenHash) Generate();
    string ComputeHash(string rawToken);
    int RefreshTokenDays { get; }
}
```

- [ ] **Step 3: `JwtTokenGenerator` (4h + claims role/isOwner)**

```csharp
using System.IdentityModel.Tokens.Jwt;
using System.Security.Claims;
using System.Text;
using HackatonFiap.Users.Application.DTOs;
using HackatonFiap.Users.Application.Interfaces;
using HackatonFiap.Users.Domain.Entities;
using Microsoft.Extensions.Configuration;
using Microsoft.IdentityModel.Tokens;

namespace HackatonFiap.Users.Infrastructure.Identity;

public class JwtTokenGenerator : IJwtTokenGenerator
{
    private readonly IConfiguration _configuration;
    public JwtTokenGenerator(IConfiguration configuration) => _configuration = configuration;

    public AccessToken GenerateAccessToken(User user)
    {
        var issuer = _configuration.GetValue<string>("Jwt:Issuer") ?? "conexaosolidaria.local";
        var audience = _configuration.GetValue<string>("Jwt:Audience") ?? "conexaosolidaria.clients";
        var secret = _configuration.GetValue<string>("Jwt:Key")
            ?? throw new InvalidOperationException("Jwt:Key must be configured via secret store/Key Vault/env — no insecure fallback.");
        if (System.Text.Encoding.UTF8.GetByteCount(secret) < 32)
            throw new InvalidOperationException("Jwt:Key must be at least 32 bytes of high-entropy data.");
        var key = new SymmetricSecurityKey(Encoding.UTF8.GetBytes(secret));

        var claims = new[]
        {
            new Claim(JwtRegisteredClaimNames.Sub, user.Id.ToString()),
            new Claim(JwtRegisteredClaimNames.Email, user.Email),
            new Claim(ClaimTypes.Role, user.Role.ToString()),
            new Claim("isOwner", user.IsOwner ? "true" : "false")
        };

        var expiry = DateTime.UtcNow.AddHours(4);
        var token = new JwtSecurityToken(
            issuer: issuer, audience: audience, claims: claims, expires: expiry,
            signingCredentials: new SigningCredentials(key, SecurityAlgorithms.HmacSha256));

        return new AccessToken(new JwtSecurityTokenHandler().WriteToken(token), expiry);
    }
}
```

> `ClaimTypes.Role` faz o `[Authorize(Roles=...)]` funcionar sem `RoleClaimType` custom. O JWT bearer no `Program.cs` deve mapear a role corretamente (já será o default com `ClaimTypes.Role`).

- [ ] **Step 4: `RefreshTokenService`**

```csharp
using System.Security.Cryptography;
using HackatonFiap.Users.Application.Interfaces;

namespace HackatonFiap.Users.Infrastructure.Identity;

public class RefreshTokenService : IRefreshTokenService
{
    public int RefreshTokenDays => 7;

    public (string RawToken, string TokenHash) Generate()
    {
        var bytes = RandomNumberGenerator.GetBytes(32);
        var raw = Convert.ToBase64String(bytes);
        return (raw, ComputeHash(raw));
    }

    public string ComputeHash(string rawToken)
    {
        var hash = SHA256.HashData(System.Text.Encoding.UTF8.GetBytes(rawToken));
        return Convert.ToHexString(hash);
    }
}
```

- [ ] **Step 5: Build** — corrigir referências a `LoginResponse`/`GenerateToken` que quebraram (Authenticate handler/tests serão reescritos na Task 8; aqui basta compilar — pode comentar temporariamente o corpo do AuthenticateUserCommandHandler ou já partir para a Task 8 antes de buildar). Recomendado: encadear direto a Task 8.

- [ ] **Step 6: Commit**

```powershell
git add -A
git commit -m "feat: servicos de access token (4h) e refresh token"
```

---

## Task 7: PRD-03 — Cadastro de Doador (`POST /api/auth/register`)

**Files:**
- Create: `src/HackatonFiap.Users.Application/Commands/RegisterDonor/{RegisterDonorCommand,RegisterDonorCommandHandler,RegisterDonorCommandValidator}.cs`
- Modify: `src/HackatonFiap.Users.Application/Interfaces/IUserRepository.cs` (+ FindByDocument, busca incluindo inativos)
- Modify: `src/HackatonFiap.Users.Infrastructure/Persistence/Repositories/UserRepository.cs`
- Modify: `src/HackatonFiap.Users.Application/Errors/UserErrors.cs`
- Modify: `src/HackatonFiap.Users.API/Controllers/AuthController.cs`
- Test: `tests/HackatonFiap.Users.UnitTests/Commands/RegisterDonorCommandHandlerTests.cs`

- [ ] **Step 1: Estender `IUserRepository` e `UserRepository`**

Interface — adicionar:
```csharp
Task<User?> FindByEmailIncludingInactiveAsync(string email);
Task<User?> FindByDocumentIncludingInactiveAsync(string documentValue);
```
Repositório:
```csharp
public Task<User?> FindByEmailIncludingInactiveAsync(string email) =>
    _db.Users.IgnoreQueryFilters().FirstOrDefaultAsync(u => u.Email == email);

public Task<User?> FindByDocumentIncludingInactiveAsync(string documentValue) =>
    _db.Users.IgnoreQueryFilters().FirstOrDefaultAsync(u => u.Document.Value == documentValue);
```

- [ ] **Step 2: Estender `UserErrors`**

```csharp
public static Error DocumentInvalid => new("User.DocumentInvalid", "Document (CPF/CNPJ) is invalid.");
public static Error DocumentAlreadyRegistered => new("User.DocumentExists", "Document already registered.");
public static Error AlreadyRegisteredActive => new("User.AlreadyRegistered", "Email or document already registered.");
public static Error AlreadyRegisteredInactive => new("User.InactiveAccount", "Account exists but is inactive. Contact an administrator to reactivate.");
public static Error Forbidden => new("User.Forbidden", "Operation not allowed for this role.");
public static Error OwnerImmutable => new("User.OwnerImmutable", "The Owner cannot be modified.");
public static Error CannotManageGestor => new("User.CannotManageGestor", "Only the Owner can manage GestorONG users.");
```

- [ ] **Step 3: Teste do handler (falhando)**

```csharp
using HackatonFiap.Users.Application.Commands.RegisterDonor;
using HackatonFiap.Users.Application.Errors;
using HackatonFiap.Users.Application.Interfaces;
using HackatonFiap.Users.Domain.Entities;
using HackatonFiap.Users.Domain.Enums;
using HackatonFiap.Users.Domain.ValueObjects;
using FluentAssertions;
using NSubstitute;
using Xunit;

namespace HackatonFiap.Users.UnitTests.Commands;

public class RegisterDonorCommandHandlerTests
{
    private readonly IUserRepository _users = Substitute.For<IUserRepository>();
    private readonly IPasswordHasher _hasher = Substitute.For<IPasswordHasher>();
    private readonly IAuditService _audit = Substitute.For<IAuditService>();
    private readonly RegisterDonorCommandHandler _handler;

    public RegisterDonorCommandHandlerTests()
    {
        _handler = new RegisterDonorCommandHandler(_users, _hasher, _audit);
        _hasher.Hash(Arg.Any<string>()).Returns("hashed");
    }

    private static RegisterDonorCommand Valid() =>
        new(PersonType.Individual, "52998224725", "João", "joao@x.com", "Senha@123", "corr");

    [Fact]
    public async Task Register_WithValidData_CreatesDonor()
    {
        _users.FindByEmailIncludingInactiveAsync(Arg.Any<string>()).Returns((User?)null);
        _users.FindByDocumentIncludingInactiveAsync(Arg.Any<string>()).Returns((User?)null);

        var result = await _handler.HandleAsync(Valid());

        result.IsSuccess.Should().BeTrue();
        result.Value.Role.Should().Be(UserRole.Doador.ToString());
        await _users.Received(1).SaveNewAsync(Arg.Is<User>(u => u.Role == UserRole.Doador));
    }

    [Fact]
    public async Task Register_WithInvalidDocument_Fails()
    {
        var cmd = Valid() with { Document = "111.111.111-11" };
        var result = await _handler.HandleAsync(cmd);
        result.Error.Code.Should().Be(UserErrors.DocumentInvalid.Code);
    }

    [Fact]
    public async Task Register_WithExistingActiveEmail_FailsAlreadyRegistered()
    {
        var existing = User.RegisterDonor(PersonType.Individual, Document.Create("52998224725", PersonType.Individual), "X", "joao@x.com", new Password("h"));
        _users.FindByEmailIncludingInactiveAsync("joao@x.com").Returns(existing);

        var result = await _handler.HandleAsync(Valid());
        result.Error.Code.Should().Be(UserErrors.AlreadyRegisteredActive.Code);
    }

    [Fact]
    public async Task Register_WithExistingInactiveEmail_FailsInactiveAccount()
    {
        var existing = User.RegisterDonor(PersonType.Individual, Document.Create("52998224725", PersonType.Individual), "X", "joao@x.com", new Password("h"));
        existing.Deactivate();
        _users.FindByEmailIncludingInactiveAsync("joao@x.com").Returns(existing);

        var result = await _handler.HandleAsync(Valid());
        result.Error.Code.Should().Be(UserErrors.AlreadyRegisteredInactive.Code);
    }
}
```

- [ ] **Step 4: Rodar e ver falhar** — `dotnet test --filter "FullyQualifiedName~RegisterDonorCommandHandlerTests"` → FAIL.

- [ ] **Step 5: Implementar Command/Validator/Handler**

```csharp
using HackatonFiap.Users.Domain.Enums;

namespace HackatonFiap.Users.Application.Commands.RegisterDonor;

public record RegisterDonorCommand(
    PersonType PersonType, string Document, string Name, string Email, string Password, string CorrelationId);
```

```csharp
using FluentValidation;

namespace HackatonFiap.Users.Application.Commands.RegisterDonor;

public class RegisterDonorCommandValidator : AbstractValidator<RegisterDonorCommand>
{
    public RegisterDonorCommandValidator()
    {
        RuleFor(x => x.Name).NotEmpty().MaximumLength(200);
        RuleFor(x => x.Email).NotEmpty().EmailAddress().MaximumLength(200);
        RuleFor(x => x.Document).NotEmpty();
        RuleFor(x => x.Password).NotEmpty();
    }
}
```

```csharp
using HackatonFiap.Users.Application.DTOs;
using HackatonFiap.Users.Application.Errors;
using HackatonFiap.Users.Application.Interfaces;
using HackatonFiap.Users.Domain.Abstractions;
using HackatonFiap.Users.Domain.Entities;
using HackatonFiap.Users.Domain.ValueObjects;

namespace HackatonFiap.Users.Application.Commands.RegisterDonor;

public class RegisterDonorCommandHandler
{
    private readonly IUserRepository _users;
    private readonly IPasswordHasher _hasher;
    private readonly IAuditService _audit;

    public RegisterDonorCommandHandler(IUserRepository users, IPasswordHasher hasher, IAuditService audit)
    {
        _users = users;
        _hasher = hasher;
        _audit = audit;
    }

    public async Task<Result<UserResponse>> HandleAsync(RegisterDonorCommand command)
    {
        if (!Password.IsValid(command.Password))
            return Result<UserResponse>.Failure(UserErrors.InvalidPassword);

        if (!Document.IsValid(command.Document, command.PersonType))
            return Result<UserResponse>.Failure(UserErrors.DocumentInvalid);

        var document = Document.Create(command.Document, command.PersonType);

        var byEmail = await _users.FindByEmailIncludingInactiveAsync(command.Email);
        var byDoc = await _users.FindByDocumentIncludingInactiveAsync(document.Value);
        var existing = byEmail ?? byDoc;
        if (existing is not null)
            return Result<UserResponse>.Failure(existing.IsActive
                ? UserErrors.AlreadyRegisteredActive
                : UserErrors.AlreadyRegisteredInactive);

        var password = new Password(_hasher.Hash(command.Password));
        var user = User.RegisterDonor(command.PersonType, document, command.Name, command.Email, password);

        await _users.SaveNewAsync(user);
        await _audit.AuditAsync("User", user.Id, "DonorRegistered", null,
            new { user.Id, user.Email, user.Document.Value }, command.CorrelationId, null);

        return Result<UserResponse>.Success(Map(user));
    }

    private static UserResponse Map(User u) => new(
        u.Id, u.PersonType.ToString(), u.Document.Value, u.Name, u.Email,
        u.Role.ToString(), u.IsActive, u.IsOwner, u.CreatedAtUtc, u.UpdatedAtUtc);
}
```

> O método `Map` se repete em vários handlers — extrair para um `UserResponse.From(User)` estático em `DTOs/UserResponse.cs` e reutilizar. (Refactor opcional; se feito, atualizar todos os handlers.)

- [ ] **Step 6: Rota no `AuthController`**

```csharp
[HttpPost("register")]
public async Task<IActionResult> Register([FromBody] RegisterDonorRequest request)
{
    var command = new RegisterDonorCommand(
        request.PersonType, request.Document, request.Name, request.Email, request.Password, _correlation.CorrelationId);
    var result = await _registerDonorHandler.HandleAsync(command);

    if (result.IsFailure)
    {
        if (result.Error.Code is "User.AlreadyRegistered" or "User.InactiveAccount")
            return Conflict(new ProblemDetails { Title = result.Error.Description, Detail = result.Error.Code });
        return BadRequest(new ProblemDetails { Title = result.Error.Description, Detail = result.Error.Code });
    }
    return StatusCode(StatusCodes.Status201Created, result.Value);
}

public record RegisterDonorRequest(PersonType PersonType, string Document, string Name, string Email, string Password);
```

Injetar `RegisterDonorCommandHandler _registerDonorHandler` no construtor do `AuthController` e registrá-lo no DI (`Program.cs`: `builder.Services.AddScoped<RegisterDonorCommandHandler>();`).

- [ ] **Step 7: Rodar e ver passar** — `dotnet test --filter "FullyQualifiedName~RegisterDonorCommandHandlerTests"` → PASS.

- [ ] **Step 8: Commit**

```powershell
git add -A
git commit -m "feat(prd-03): cadastro de doador PF/PJ com unicidade incluindo inativos"
```

---

## Task 8: PRD-01 — Login com access + refresh (`POST /api/auth/login`)

**Files:**
- Modify: `src/HackatonFiap.Users.Application/Commands/AuthenticateUser/AuthenticateUserCommandHandler.cs`
- Modify: `src/HackatonFiap.Users.API/Controllers/AuthController.cs`
- Modify/Recreate: `tests/.../Commands/AuthenticateUserCommandHandlerTests.cs`

- [ ] **Step 1: Reescrever o teste (falhando)**

```csharp
using HackatonFiap.Users.Application.Commands.AuthenticateUser;
using HackatonFiap.Users.Application.DTOs;
using HackatonFiap.Users.Application.Errors;
using HackatonFiap.Users.Application.Interfaces;
using HackatonFiap.Users.Domain.Entities;
using HackatonFiap.Users.Domain.Enums;
using HackatonFiap.Users.Domain.ValueObjects;
using FluentAssertions;
using NSubstitute;
using Xunit;

namespace HackatonFiap.Users.UnitTests.Commands;

public class AuthenticateUserCommandHandlerTests
{
    private readonly IUserRepository _users = Substitute.For<IUserRepository>();
    private readonly IPasswordHasher _hasher = Substitute.For<IPasswordHasher>();
    private readonly IJwtTokenGenerator _jwt = Substitute.For<IJwtTokenGenerator>();
    private readonly IRefreshTokenRepository _refresh = Substitute.For<IRefreshTokenRepository>();
    private readonly IRefreshTokenService _refreshSvc = Substitute.For<IRefreshTokenService>();
    private readonly AuthenticateUserCommandHandler _handler;

    public AuthenticateUserCommandHandlerTests()
    {
        _handler = new AuthenticateUserCommandHandler(_users, _hasher, _jwt, _refresh, _refreshSvc);
        _jwt.GenerateAccessToken(Arg.Any<User>()).Returns(new AccessToken("jwt", DateTime.UtcNow.AddHours(4)));
        _refreshSvc.Generate().Returns(("raw", "hash"));
        _refreshSvc.RefreshTokenDays.Returns(7);
    }

    private static User ActiveUser() =>
        User.RegisterDonor(PersonType.Individual, Document.Create("52998224725", PersonType.Individual), "J", "j@x.com", new Password("hashed"));

    [Fact]
    public async Task Login_Valid_ReturnsTokensAndPersistsRefresh()
    {
        var user = ActiveUser();
        _users.FindByEmailAsync("j@x.com").Returns(user);
        _hasher.Verify("Senha@123", "hashed").Returns(true);

        var result = await _handler.HandleAsync(new AuthenticateUserCommand("j@x.com", "Senha@123", "corr"));

        result.IsSuccess.Should().BeTrue();
        result.Value.AccessToken.Should().Be("jwt");
        result.Value.RefreshToken.Should().Be("raw");
        await _refresh.Received(1).AddAsync(Arg.Any<Domain.Entities.RefreshToken>());
    }

    [Fact]
    public async Task Login_WrongPassword_Fails()
    {
        var user = ActiveUser();
        _users.FindByEmailAsync("j@x.com").Returns(user);
        _hasher.Verify(Arg.Any<string>(), Arg.Any<string>()).Returns(false);

        var result = await _handler.HandleAsync(new AuthenticateUserCommand("j@x.com", "x", "corr"));
        result.Error.Code.Should().Be(UserErrors.InvalidCredentials.Code);
    }

    [Fact]
    public async Task Login_InactiveUser_Fails()
    {
        var user = ActiveUser();
        user.Deactivate();
        _users.FindByEmailAsync("j@x.com").Returns(user);
        _hasher.Verify(Arg.Any<string>(), Arg.Any<string>()).Returns(true);

        var result = await _handler.HandleAsync(new AuthenticateUserCommand("j@x.com", "x", "corr"));
        result.Error.Code.Should().Be(UserErrors.InvalidCredentials.Code);
    }
}
```

> Nota: `FindByEmailAsync` deve enxergar inativos para o teste de inativo cair na regra de IsActive (e não em "not found"). Ajustar `UserRepository.FindByEmailAsync` para usar `IgnoreQueryFilters()` (login precisa distinguir inativo de inexistente). Atualizar a doc se necessário.

- [ ] **Step 2: Rodar e ver falhar** — FAIL (ctor e métodos mudaram).

- [ ] **Step 3: Reescrever `AuthenticateUserCommandHandler`**

```csharp
using HackatonFiap.Users.Application.DTOs;
using HackatonFiap.Users.Application.Errors;
using HackatonFiap.Users.Application.Interfaces;
using HackatonFiap.Users.Domain.Abstractions;
using HackatonFiap.Users.Domain.Entities;

namespace HackatonFiap.Users.Application.Commands.AuthenticateUser;

public class AuthenticateUserCommandHandler
{
    private readonly IUserRepository _users;
    private readonly IPasswordHasher _hasher;
    private readonly IJwtTokenGenerator _jwt;
    private readonly IRefreshTokenRepository _refresh;
    private readonly IRefreshTokenService _refreshSvc;

    public AuthenticateUserCommandHandler(
        IUserRepository users, IPasswordHasher hasher, IJwtTokenGenerator jwt,
        IRefreshTokenRepository refresh, IRefreshTokenService refreshSvc)
    {
        _users = users; _hasher = hasher; _jwt = jwt; _refresh = refresh; _refreshSvc = refreshSvc;
    }

    public async Task<Result<AuthResponse>> HandleAsync(AuthenticateUserCommand command)
    {
        var user = await _users.FindByEmailAsync(command.Email);
        if (user is null || !_hasher.Verify(command.Password, user.Password.HashValue) || !user.IsActive)
            return Result<AuthResponse>.Failure(UserErrors.InvalidCredentials);

        var access = _jwt.GenerateAccessToken(user);
        var (raw, hash) = _refreshSvc.Generate();
        var refreshToken = RefreshToken.Issue(user.Id, hash, DateTime.UtcNow.AddDays(_refreshSvc.RefreshTokenDays));
        await _refresh.AddAsync(refreshToken);

        var expiresIn = (int)(access.ExpiresAtUtc - DateTime.UtcNow).TotalSeconds;
        return Result<AuthResponse>.Success(new AuthResponse(access.Token, raw, expiresIn));
    }
}
```

- [ ] **Step 4: Atualizar `AuthController.Login`** para devolver `AuthResponse` (já é `Ok(result.Value)`; ajustar DI: injetar os 4 handlers de auth). Garantir `AddScoped<AuthenticateUserCommandHandler>()` mantido.

- [ ] **Step 5: Rodar e ver passar** — `dotnet test --filter "FullyQualifiedName~AuthenticateUserCommandHandlerTests"` → PASS.

- [ ] **Step 6: Commit**

```powershell
git add -A
git commit -m "feat(prd-01): login emite access(4h)+refresh(7d) e bloqueia inativo"
```

---

## Task 9: PRD-01 — Refresh com rotação e detecção de reuso (`POST /api/auth/refresh`)

**Files:**
- Create: `src/HackatonFiap.Users.Application/Commands/RefreshToken/{RefreshTokenCommand,RefreshTokenCommandHandler}.cs`
- Modify: `AuthController`, DI
- Test: `tests/.../Commands/RefreshTokenCommandHandlerTests.cs`

- [ ] **Step 1: Teste (falhando)** — cobrir: refresh válido rota; reuso (token revogado) revoga cadeia + falha; expirado falha.

```csharp
using HackatonFiap.Users.Application.Commands.RefreshTokenFlow;
using HackatonFiap.Users.Application.Errors;
using HackatonFiap.Users.Application.Interfaces;
using HackatonFiap.Users.Domain.Entities;
using HackatonFiap.Users.Domain.Enums;
using HackatonFiap.Users.Domain.ValueObjects;
using HackatonFiap.Users.Application.DTOs;
using FluentAssertions;
using NSubstitute;
using Xunit;

namespace HackatonFiap.Users.UnitTests.Commands;

public class RefreshTokenCommandHandlerTests
{
    private readonly IUserRepository _users = Substitute.For<IUserRepository>();
    private readonly IRefreshTokenRepository _refresh = Substitute.For<IRefreshTokenRepository>();
    private readonly IRefreshTokenService _refreshSvc = Substitute.For<IRefreshTokenService>();
    private readonly IJwtTokenGenerator _jwt = Substitute.For<IJwtTokenGenerator>();
    private readonly RefreshTokenCommandHandler _handler;

    public RefreshTokenCommandHandlerTests()
    {
        _handler = new RefreshTokenCommandHandler(_users, _refresh, _refreshSvc, _jwt);
        _refreshSvc.ComputeHash("raw").Returns("hash");
        _refreshSvc.Generate().Returns(("raw2", "hash2"));
        _refreshSvc.RefreshTokenDays.Returns(7);
        _jwt.GenerateAccessToken(Arg.Any<User>()).Returns(new AccessToken("jwt", DateTime.UtcNow.AddHours(4)));
    }

    private static User U() => User.RegisterDonor(PersonType.Individual, Document.Create("52998224725", PersonType.Individual), "J", "j@x.com", new Password("h"));

    [Fact]
    public async Task Refresh_Valid_RotatesAndReturnsNewPair()
    {
        var user = U();
        var token = RefreshToken.Issue(user.Id, "hash", DateTime.UtcNow.AddDays(7));
        _refresh.FindByHashAsync("hash").Returns(token);
        _users.FindByIdAsync(user.Id).Returns(user);

        var result = await _handler.HandleAsync(new RefreshTokenCommand("raw", "corr"));

        result.IsSuccess.Should().BeTrue();
        result.Value.RefreshToken.Should().Be("raw2");
        token.RevokedAtUtc.Should().NotBeNull();           // atual revogado
        await _refresh.Received(1).AddAsync(Arg.Any<RefreshToken>());
    }

    [Fact]
    public async Task Refresh_ReusedRevokedToken_RevokesChainAndFails()
    {
        var user = U();
        var revoked = RefreshToken.Issue(user.Id, "hash", DateTime.UtcNow.AddDays(7));
        revoked.Revoke(Guid.NewGuid());
        _refresh.FindByHashAsync("hash").Returns(revoked);

        var result = await _handler.HandleAsync(new RefreshTokenCommand("raw", "corr"));

        result.Error.Code.Should().Be(UserErrors.InvalidRefreshToken.Code);
        await _refresh.Received(1).RevokeAllForUserAsync(user.Id);
    }
}
```

> Nota: o namespace da pasta `RefreshToken` colide com a entidade `RefreshToken`. Usar namespace de pasta `RefreshTokenFlow` (ou `Auth.Refresh`) para os comandos, evitando ambiguidade. Adicionar `Task RevokeAllForUserAsync(Guid userId)` em `IRefreshTokenRepository` + impl no repo (revoga todos os ativos do usuário e `SaveChanges`).

- [ ] **Step 2: Rodar e ver falhar** — FAIL.

- [ ] **Step 3: Implementar Command/Handler + `UserErrors.InvalidRefreshToken` + `RevokeAllForUserAsync`**

`UserErrors`: `public static Error InvalidRefreshToken => new("Auth.InvalidRefreshToken", "Refresh token is invalid, expired or revoked.");`

`IRefreshTokenRepository` + `RefreshTokenRepository`:
```csharp
public async Task RevokeAllForUserAsync(Guid userId)
{
    var active = await _db.RefreshTokens
        .Where(t => t.UserId == userId && t.RevokedAtUtc == null)
        .ToListAsync();
    foreach (var t in active) t.Revoke();
    await _db.SaveChangesAsync();
}
```

```csharp
using HackatonFiap.Users.Application.DTOs;
using HackatonFiap.Users.Application.Errors;
using HackatonFiap.Users.Application.Interfaces;
using HackatonFiap.Users.Domain.Abstractions;
using HackatonFiap.Users.Domain.Entities;

namespace HackatonFiap.Users.Application.Commands.RefreshTokenFlow;

public record RefreshTokenCommand(string RefreshToken, string CorrelationId);

public class RefreshTokenCommandHandler
{
    private readonly IUserRepository _users;
    private readonly IRefreshTokenRepository _refresh;
    private readonly IRefreshTokenService _refreshSvc;
    private readonly IJwtTokenGenerator _jwt;

    public RefreshTokenCommandHandler(IUserRepository users, IRefreshTokenRepository refresh,
        IRefreshTokenService refreshSvc, IJwtTokenGenerator jwt)
    {
        _users = users; _refresh = refresh; _refreshSvc = refreshSvc; _jwt = jwt;
    }

    public async Task<Result<AuthResponse>> HandleAsync(RefreshTokenCommand command)
    {
        var hash = _refreshSvc.ComputeHash(command.RefreshToken);
        var stored = await _refresh.FindByHashAsync(hash);

        if (stored is null)
            return Result<AuthResponse>.Failure(UserErrors.InvalidRefreshToken);

        // reuso de token já revogado/rotacionado → revoga toda a cadeia (RN01.12)
        if (!stored.IsActive)
        {
            await _refresh.RevokeAllForUserAsync(stored.UserId);
            return Result<AuthResponse>.Failure(UserErrors.InvalidRefreshToken);
        }

        var user = await _users.FindByIdAsync(stored.UserId);
        if (user is null || !user.IsActive)
            return Result<AuthResponse>.Failure(UserErrors.InvalidRefreshToken);

        var (raw, newHash) = _refreshSvc.Generate();
        var newToken = RefreshToken.Issue(user.Id, newHash, DateTime.UtcNow.AddDays(_refreshSvc.RefreshTokenDays));
        stored.Revoke(newToken.Id);
        await _refresh.AddAsync(newToken);
        await _refresh.SaveChangesAsync();

        var access = _jwt.GenerateAccessToken(user);
        var expiresIn = (int)(access.ExpiresAtUtc - DateTime.UtcNow).TotalSeconds;
        return Result<AuthResponse>.Success(new AuthResponse(access.Token, raw, expiresIn));
    }
}
```

> Refresh usa o `FindByIdAsync` **filtrado** (respeita o soft-delete): usuário inativo → `null` → `InvalidRefreshToken`. **Não** burlar o query filter no caminho de auth (achado de segurança HIGH). O check `!user.IsActive` no handler fica como defesa-em-profundidade.

- [ ] **Step 4: Rota `POST /api/auth/refresh`** no `AuthController` (público): mapeia falha → `Unauthorized`.

- [ ] **Step 5: Rodar e ver passar** — PASS.

- [ ] **Step 6: Commit**

```powershell
git add -A
git commit -m "feat(prd-01): refresh rotativo com deteccao de reuso (revoga cadeia)"
```

---

## Task 10: PRD-01 — Logout (`POST /api/auth/logout`)

**Files:**
- Create: `src/HackatonFiap.Users.Application/Commands/Logout/{LogoutCommand,LogoutCommandHandler}.cs`
- Modify: `AuthController`, DI
- Test: `tests/.../Commands/LogoutCommandHandlerTests.cs`

- [ ] **Step 1: Teste (falhando)** — logout de token existente revoga; token inexistente é no-op idempotente (204).

```csharp
[Fact]
public async Task Logout_RevokesToken()
{
    var token = RefreshToken.Issue(Guid.NewGuid(), "hash", DateTime.UtcNow.AddDays(7));
    _refreshSvc.ComputeHash("raw").Returns("hash");
    _refresh.FindByHashAsync("hash").Returns(token);

    var result = await _handler.HandleAsync(new LogoutCommand("raw", "corr"));

    result.IsSuccess.Should().BeTrue();
    token.RevokedAtUtc.Should().NotBeNull();
    await _refresh.Received(1).SaveChangesAsync();
}
```

- [ ] **Step 2: Rodar e ver falhar** — FAIL.

- [ ] **Step 3: Implementar**

```csharp
using HackatonFiap.Users.Application.Interfaces;
using HackatonFiap.Users.Domain.Abstractions;

namespace HackatonFiap.Users.Application.Commands.Logout;

public record LogoutCommand(string RefreshToken, string CorrelationId);

public class LogoutCommandHandler
{
    private readonly IRefreshTokenRepository _refresh;
    private readonly IRefreshTokenService _refreshSvc;

    public LogoutCommandHandler(IRefreshTokenRepository refresh, IRefreshTokenService refreshSvc)
    {
        _refresh = refresh; _refreshSvc = refreshSvc;
    }

    public async Task<Result> HandleAsync(LogoutCommand command)
    {
        var hash = _refreshSvc.ComputeHash(command.RefreshToken);
        var stored = await _refresh.FindByHashAsync(hash);
        if (stored is not null && stored.IsActive)
        {
            stored.Revoke();
            await _refresh.SaveChangesAsync();
        }
        return Result.Success();
    }
}
```

- [ ] **Step 4: Rota `POST /api/auth/logout`** `[Authorize]` → `NoContent()`.

- [ ] **Step 5: Rodar e ver passar** — PASS.

- [ ] **Step 6: Commit**

```powershell
git add -A
git commit -m "feat(prd-01): logout revoga refresh token"
```

---

## Task 11: PRD-02 — Gestão de usuários + matriz de permissões + Owner seed

**Files:**
- Create: Commands `CreateUser` (reescrito), `UpdateUser`, `ChangeUserRole`, `DeactivateUser`, `ReactivateUser`, `UpdateMyProfile`, `ResetMyPassword`.
- Create: Queries `GetUserById`, `ListUsers`; manter `GetProfile`.
- Modify: `IUserRepository` (+ `ListAsync`, `FindOwnerAsync`), `UserRepository`.
- Modify: `UsersController`, `Program.cs` (DI + seed Owner).
- Test: arquivos de teste por handler.

**Contexto de autorização nos handlers:** cada command de gestão recebe o **chamador** (`CallerId`, `CallerRole`, `CallerIsOwner`) extraído do JWT pelo controller. As regras (matriz PRD-02) são validadas no handler e retornam `Result.Failure(UserErrors.Forbidden|OwnerImmutable|CannotManageGestor)` → controller mapeia para **403**.

- [ ] **Step 1: Estender `IUserRepository`/`UserRepository`**

Interface: `Task<IReadOnlyList<User>> ListAsync();` e `Task<User?> FindOwnerAsync();`
Repo:
```csharp
public async Task<IReadOnlyList<User>> ListAsync() =>
    await _db.Users.IgnoreQueryFilters().AsNoTracking().OrderBy(u => u.Name).ToListAsync();

public Task<User?> FindOwnerAsync() =>
    _db.Users.IgnoreQueryFilters().FirstOrDefaultAsync(u => u.IsOwner);
```
Adicionar um método **dedicado** para a gestão enxergar inativos (NÃO alterar `FindByIdAsync`, usado no caminho de auth e que deve permanecer **filtrado**): `Task<User?> FindByIdIncludingInactiveAsync(Guid id)` → `_db.Users.IgnoreQueryFilters().FirstOrDefaultAsync(u => u.Id == id)`. Os handlers de gestão (Deactivate/Reactivate/ChangeUserRole/UpdateUser/GetUserById) usam `FindByIdIncludingInactiveAsync`.

- [ ] **Step 2: `DeactivateUser` — teste (falhando)**

Regras: Owner nunca (OwnerImmutable); GestorONG só pode inativar Doador a menos que caller seja Owner; ao inativar, **revoga refresh tokens** (RN01.11).

```csharp
[Fact]
public async Task Deactivate_Owner_FailsImmutable()
{
    var owner = User.CreateOwner(Document.Create("52998224725", PersonType.Individual), "O", "o@x.com", new Password("h"));
    _users.FindByIdAsync(owner.Id).Returns(owner);

    var cmd = new DeactivateUserCommand(owner.Id, CallerIsOwner: true, "corr");
    var result = await _handler.HandleAsync(cmd);

    result.Error.Code.Should().Be(UserErrors.OwnerImmutable.Code);
}

[Fact]
public async Task Deactivate_DoadorByGestor_RevokesRefreshTokens()
{
    var doador = User.RegisterDonor(PersonType.Individual, Document.Create("52998224725", PersonType.Individual), "D", "d@x.com", new Password("h"));
    _users.FindByIdAsync(doador.Id).Returns(doador);

    var cmd = new DeactivateUserCommand(doador.Id, CallerIsOwner: false, "corr");
    var result = await _handler.HandleAsync(cmd);

    result.IsSuccess.Should().BeTrue();
    doador.IsActive.Should().BeFalse();
    await _refresh.Received(1).RevokeAllForUserAsync(doador.Id);
}
```

- [ ] **Step 3: Implementar `DeactivateUser`**

```csharp
using HackatonFiap.Users.Application.Errors;
using HackatonFiap.Users.Application.Interfaces;
using HackatonFiap.Users.Domain.Abstractions;
using HackatonFiap.Users.Domain.Enums;

namespace HackatonFiap.Users.Application.Commands.DeactivateUser;

public record DeactivateUserCommand(Guid UserId, bool CallerIsOwner, string CorrelationId);

public class DeactivateUserCommandHandler
{
    private readonly IUserRepository _users;
    private readonly IRefreshTokenRepository _refresh;

    public DeactivateUserCommandHandler(IUserRepository users, IRefreshTokenRepository refresh)
    {
        _users = users; _refresh = refresh;
    }

    public async Task<Result> HandleAsync(DeactivateUserCommand command)
    {
        var user = await _users.FindByIdAsync(command.UserId);
        if (user is null) return Result.Failure(UserErrors.NotFound);
        if (user.IsOwner) return Result.Failure(UserErrors.OwnerImmutable);
        if (user.Role == UserRole.GestorONG && !command.CallerIsOwner)
            return Result.Failure(UserErrors.CannotManageGestor);

        user.Deactivate();
        await _users.UpdateAsync(user);
        await _refresh.RevokeAllForUserAsync(user.Id);
        return Result.Success();
    }
}
```

- [ ] **Step 4: `ReactivateUser`** (mesma matriz; sem revogar tokens). Teste + impl análogos: Owner não se aplica (Owner nunca inativo); GestorONG-alvo só por Owner.

- [ ] **Step 5: `CreateUser` (admin)** — teste + impl. Regra: `CallerIsOwner=false` só cria `Doador`; criar `GestorONG` exige Owner (senão `CannotManageGestor`). Valida documento/unicidade como na Task 7. Reusar a lógica de unicidade.

```csharp
public record CreateUserCommand(PersonType PersonType, string Document, string Name, string Email, string Password, UserRole Role, bool CallerIsOwner, string CorrelationId);
```
Handler: se `Role == GestorONG && !CallerIsOwner` → `CannotManageGestor`. Caso ok, `User.CreateByAdmin(...)`.

- [ ] **Step 6: `ChangeUserRole`** — **somente Owner**. Teste + impl: `if (!CallerIsOwner) return Forbidden;` carrega user, `user.ChangeRole(novaRole)`, salva. Owner-alvo não pode ser rebaixado (`OwnerImmutable`).

- [ ] **Step 7: `UpdateUser`** (admin edita `Name`) + `UpdateMyProfile` (self edita só `Name`) + `ResetMyPassword` (self: valida senha atual com `IPasswordHasher.Verify`, valida nova com `Password.IsValid`, troca). Testes + impl.

```csharp
public record ResetMyPasswordCommand(Guid UserId, string CurrentPassword, string NewPassword, string CorrelationId);
// handler: FindById; Verify(current, hash) senão InvalidCredentials; Password.IsValid(new) senão InvalidPassword; user.ChangePassword(new hash); Update.
```

- [ ] **Step 8: Queries `GetUserById` + `ListUsers`** (mapeiam para `UserResponse`/lista). `GetProfile` permanece (ajustar Map para o novo `UserResponse`).

- [ ] **Step 9: Reescrever `UsersController`** com todas as rotas (extrair claims do caller):

```csharp
private (Guid id, bool isOwner) Caller()
{
    var id = Guid.Parse(User.FindFirst("sub")!.Value);
    var isOwner = User.FindFirst("isOwner")?.Value == "true";
    return (id, isOwner);
}
```
Rotas: `POST /api/users` `[Authorize(Roles="GestorONG")]`; `PUT /api/users/{id}`; `PATCH /api/users/{id}/deactivate`; `PATCH /api/users/{id}/reactivate`; `PATCH /api/users/{id}/role` (ChangeUserRole); `GET /api/users` `[Authorize(Roles="GestorONG")]`; `GET /api/users/me` `[Authorize]`; `PUT /api/users/me`; `POST /api/users/me/reset-password`. Mapear `Forbidden|OwnerImmutable|CannotManageGestor` → `403`; `NotFound` → `404`.

- [ ] **Step 10: Seed do Owner no `Program.cs`** — substitui o seed antigo:

```csharp
if (!db.Users.IgnoreQueryFilters().Any(u => u.IsOwner))
{
    var hasher = scope.ServiceProvider.GetRequiredService<IPasswordHasher>();
    var isDev = app.Environment.IsDevelopment();
    var ownerEmail = configuration.GetValue<string>("Owner:Email") ?? "owner@conexaosolidaria.org";
    // Senha do Owner: em produção é OBRIGATÓRIA via secret/Key Vault (fail-fast); fallback só em Development.
    var ownerPwd = configuration.GetValue<string>("Owner:Password")
        ?? (isDev ? "Owner@123" : throw new InvalidOperationException("Owner:Password must be configured (Key Vault/secret) outside Development."));
    var ownerDoc = configuration.GetValue<string>("Owner:Document") ?? "52998224725";
    var owner = User.CreateOwner(
        Document.Create(ownerDoc, PersonType.Individual),
        configuration.GetValue<string>("Owner:Name") ?? "Owner",
        ownerEmail,
        new Password(hasher.Hash(ownerPwd)));
    db.Users.Add(owner);
    db.SaveChanges();
}
```

- [ ] **Step 11: DI** — registrar todos os novos handlers/queries e `IRefreshTokenRepository`/`IRefreshTokenService` no `Program.cs`:

```csharp
builder.Services.AddScoped<IRefreshTokenRepository, RefreshTokenRepository>();
builder.Services.AddSingleton<IRefreshTokenService, RefreshTokenService>();
builder.Services.AddScoped<RegisterDonorCommandHandler>();
builder.Services.AddScoped<RefreshTokenCommandHandler>();
builder.Services.AddScoped<LogoutCommandHandler>();
builder.Services.AddScoped<CreateUserCommandHandler>();
builder.Services.AddScoped<UpdateUserCommandHandler>();
builder.Services.AddScoped<ChangeUserRoleCommandHandler>();
builder.Services.AddScoped<DeactivateUserCommandHandler>();
builder.Services.AddScoped<ReactivateUserCommandHandler>();
builder.Services.AddScoped<UpdateMyProfileCommandHandler>();
builder.Services.AddScoped<ResetMyPasswordCommandHandler>();
builder.Services.AddScoped<GetUserByIdQueryHandler>();
builder.Services.AddScoped<ListUsersQueryHandler>();
```

- [ ] **Step 11b: Hardening de segredos (resolve achados do security review)**

No bloco de autenticação JWT do `Program.cs`, remover o fallback inseguro da chave e falhar-rápido:
```csharp
var jwtKey = configuration.GetValue<string>("Jwt:Key")
    ?? throw new InvalidOperationException("Jwt:Key must be configured (Key Vault/secret/env).");
if (System.Text.Encoding.UTF8.GetByteCount(jwtKey) < 32)
    throw new InvalidOperationException("Jwt:Key must be at least 32 bytes.");
```
Nenhuma string secreta hardcoded em código (a chave de dev fica em `appsettings.Development.json` — ver Task 14). O seed do Owner já está gated para Development (Step 10).

- [ ] **Step 12: Build + todos os testes**

Run: `dotnet test`
Expected: PASS (todos).

- [ ] **Step 13: Commit**

```powershell
git add -A
git commit -m "feat(prd-02): gestao de usuarios, matriz de permissoes e seed do Owner"
```

---

## Task 12: Observabilidade — OpenTelemetry (traces + métricas) + `/metrics`

**Files:**
- Modify: `src/HackatonFiap.Users.API/HackatonFiap.Users.API.csproj` (+pacotes OTel)
- Create: `src/HackatonFiap.Users.API/Observability/Telemetry.cs`
- Modify: `src/HackatonFiap.Users.API/Program.cs`

- [ ] **Step 1: Adicionar pacotes ao `.csproj` da API**

```xml
<PackageReference Include="OpenTelemetry.Extensions.Hosting" Version="1.9.0" />
<PackageReference Include="OpenTelemetry.Instrumentation.AspNetCore" Version="1.9.0" />
<PackageReference Include="OpenTelemetry.Instrumentation.Http" Version="1.9.0" />
<PackageReference Include="OpenTelemetry.Instrumentation.EntityFrameworkCore" Version="1.0.0-beta.12" />
<PackageReference Include="OpenTelemetry.Instrumentation.Runtime" Version="1.9.0" />
<PackageReference Include="OpenTelemetry.Exporter.Prometheus.AspNetCore" Version="1.9.0-beta.2" />
```

- [ ] **Step 2: `Telemetry.cs` (ActivitySource + Meter + contadores custom)**

```csharp
using System.Diagnostics;
using System.Diagnostics.Metrics;

namespace HackatonFiap.Users.API.Observability;

public static class Telemetry
{
    public const string ServiceName = "HackatonFiap.Users";
    public static readonly ActivitySource ActivitySource = new(ServiceName);
    public static readonly Meter Meter = new(ServiceName);

    public static readonly Counter<long> LoginsTotal =
        Meter.CreateCounter<long>("users_logins_total", description: "Total de logins bem-sucedidos");
    public static readonly Counter<long> RegistrationsTotal =
        Meter.CreateCounter<long>("users_registrations_total", description: "Total de cadastros de doador");
}
```

- [ ] **Step 3: Wiring no `Program.cs`**

```csharp
using OpenTelemetry.Metrics;
using OpenTelemetry.Resources;
using OpenTelemetry.Trace;
using HackatonFiap.Users.API.Observability;

var otelResource = ResourceBuilder.CreateDefault().AddService(Telemetry.ServiceName);

builder.Services.AddOpenTelemetry()
    .WithTracing(t => t
        .SetResourceBuilder(otelResource)
        .AddSource(Telemetry.ServiceName)
        .AddAspNetCoreInstrumentation()
        .AddHttpClientInstrumentation()
        .AddEntityFrameworkCoreInstrumentation())
    .WithMetrics(m => m
        .SetResourceBuilder(otelResource)
        .AddMeter(Telemetry.ServiceName)
        .AddAspNetCoreInstrumentation()
        .AddRuntimeInstrumentation()
        .AddPrometheusExporter());

// ... após app.Build() e antes de app.Run():
app.MapPrometheusScrapingEndpoint("/metrics");
```

Enriquecer o Serilog com trace/span (na config do Serilog já existente, adicionar `.Enrich.WithSpan()` se usar `Serilog.Enrichers.Span`, ou logar `Activity.Current?.TraceId`). Opcional, mas recomendado para casar logs↔traces.

- [ ] **Step 4: Incrementar contadores** — em `AuthenticateUserCommandHandler` (sucesso) e `RegisterDonorCommandHandler`. Como os handlers estão na Application (sem dependência da API), criar uma abstração `IMetrics` na Application com impl na API/Infra **ou** incrementar no controller após sucesso. **Decisão:** incrementar nos controllers (`AuthController`/`Register`) após `IsSuccess`, mantendo Application livre de OTel.

```csharp
if (result.IsSuccess) Telemetry.LoginsTotal.Add(1);
```

- [ ] **Step 5: Build + rodar a API e validar `/metrics`**

Run: `dotnet run --project src/HackatonFiap.Users.API` e, em outro terminal, `curl http://localhost:5000/metrics`
Expected: corpo no formato Prometheus contendo `users_logins_total` e métricas de runtime/aspnetcore.

- [ ] **Step 6: Commit**

```powershell
git add -A
git commit -m "feat(obs): OpenTelemetry traces+metricas e endpoint /metrics (Prometheus)"
```

---

## Task 13: Migration `InitialCreate` (regenerada)

**Files:**
- Remove: `src/HackatonFiap.Users.Infrastructure/Migrations/*` antigas (e `Migrations/` na raiz da API, se houver duplicata) — boilerplate de hackathon, sem dados legados.
- Create: nova migration `InitialCreate`.

- [ ] **Step 1: Remover migrations antigas**

```powershell
Remove-Item -Recurse -Force "src/HackatonFiap.Users.Infrastructure/Migrations"
```

- [ ] **Step 2: Gerar a migration**

Run:
```powershell
dotnet ef migrations add InitialCreate --project src/HackatonFiap.Users.Infrastructure --startup-project src/HackatonFiap.Users.API
```
Expected: cria `Migrations/<timestamp>_InitialCreate.cs` com tabelas `Users` (Document único, Email único, PersonType/Role string, IsActive/IsOwner) e `RefreshTokens` (TokenHash único, índice UserId).

- [ ] **Step 3: Validar contra um SQL Server local**

Run (com SQL Server do `docker-compose` no ar):
```powershell
dotnet ef database update --project src/HackatonFiap.Users.Infrastructure --startup-project src/HackatonFiap.Users.API
```
Expected: aplica sem erros; tabelas criadas.

- [ ] **Step 4: Commit**

```powershell
git add -A
git commit -m "chore(db): regenera migration InitialCreate (Users + RefreshTokens)"
```

---

## Task 14: Documentação, config e CI

**Files:**
- Modify: `CLAUDE.md`, `README.md`, `appsettings.json`, `docker-compose.yml`, `k8s/*`, `.github/workflows/*`
- Modify (vault): `docs/01 - Requisitos/Requisitos Técnicos.md` (test lib = NSubstitute)

- [ ] **Step 1: `appsettings.json` + `appsettings.Development.json`** — **NÃO** commitar segredos no `appsettings.json` (sem `Jwt:Key` nem `Owner:Password` reais — ausentes/vazios para forçar fail-fast em produção). A chave JWT de **desenvolvimento** (≥32 bytes) e o `Owner:Password` de dev ficam **apenas** em `appsettings.Development.json` (dev-only). Em produção, `Jwt:Key`/`Owner:Password` vêm de Key Vault/secret (RNF21/RN02.8). Adicionar a seção `Owner` (Email/Document/Name, **sem senha**) no `appsettings.json`.

- [ ] **Step 2: `CLAUDE.md`** — atualizar endpoints (`/api/auth/register|login|refresh|logout`, `/api/users...`), entidades (`User`, `RefreshToken`, `Document`), roles `Doador`/`GestorONG`, libs de teste (NSubstitute), comando de migration, `/metrics`.

- [ ] **Step 3: `README.md`** — passo a passo: `docker compose up`, migrations, login com Owner seed, exemplos de payload (register PF/PJ, login, refresh), `/health` e `/metrics`.

- [ ] **Step 4: CI (`.github/workflows`)** — garantir steps: `dotnet restore/build/test` + build da imagem Docker. Ajustar nomes que referenciem `FGC.Users`.

- [ ] **Step 5: k8s / docker-compose** — atualizar nomes de imagem/serviço para `hackatonfiap-users`; ConfigMap/Secret com `Jwt`, `ConnectionStrings:Default`, `Owner:*`, `ApplicationInsights`. Conferir probes `/health` e scrape `/metrics`.

- [ ] **Step 6: Atualizar o vault** — em `docs/01 - Requisitos/Requisitos Técnicos.md`, §9: trocar "NSubstitute" confirmado (já está) e garantir consistência; registrar que a UserAPI não publica eventos cross-service.

- [ ] **Step 7: Build + testes finais**

Run: `dotnet build` e `dotnet test`
Expected: build succeeded; todos os testes PASS.

- [ ] **Step 8: Commit + push**

```powershell
git add -A
git commit -m "docs/chore: atualiza CLAUDE/README/CI/k8s e config para Conexao Solidaria"
git push -u origin feature/userapi-conexao-solidaria
```

---

## Self-Review (cobertura do spec)

- **PRD-03** (cadastro Doador, CPF/CNPJ, unicidade incl. inativos, 201 sem token) → Tasks 3, 7. ✅
- **PRD-01** (login access 4h + refresh 7d, refresh rotativo + reuso, logout, inativo não autentica) → Tasks 6, 8, 9, 10. ✅
- **PRD-02** (Owner seed, matriz de permissões, soft-delete + revogação de refresh, reativação, troca de role só Owner, self profile/reset-password, listar/detalhar) → Task 11. ✅
- **Observabilidade OpenTelemetry + `/metrics`** → Task 12. ✅
- **Stack** (.NET 8, NSubstitute) → Tasks 1; .NET 8 já vigente. ✅
- **Rename / namespace** → Task 0. ✅
- **Persistência/migrations** → Tasks 4, 5, 13. ✅
- **Sem eventos cross-service** → remoção do publish é coberta ao reescrever handlers (Tasks 7/8) e DI (Task 11): não registrar `IEventPublisher`/`ServiceBus`. Adicionar nota explícita na Task 11/14. ✅

**Consistência de tipos:** `AuthResponse(AccessToken, RefreshToken, ExpiresIn)`, `AccessToken(Token, ExpiresAtUtc)`, `IJwtTokenGenerator.GenerateAccessToken`, `IRefreshTokenService.{Generate,ComputeHash,RefreshTokenDays}`, `IRefreshTokenRepository.{AddAsync,FindByHashAsync,FindActiveByUserAsync,RevokeAllForUserAsync,SaveChangesAsync}`, `IUserRepository.{FindByIdAsync,FindByEmailAsync,FindByEmailIncludingInactiveAsync,FindByDocumentIncludingInactiveAsync,ListAsync,FindOwnerAsync,SaveNewAsync,UpdateAsync}` — usados de forma consistente entre tasks. Namespace dos comandos de refresh = `RefreshTokenFlow` (evita colisão com a entidade `RefreshToken`).

**Pontos de atenção registrados:** `FindByEmailAsync`/`FindByIdAsync` usam `IgnoreQueryFilters()` (login/refresh/gestão precisam distinguir inativo de inexistente); índice único em owned type exige EF Core 8.
